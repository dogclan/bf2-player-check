export enum Project {
    bf2hub = 0,
    playbf2
}

export enum LeaderboardCategory {
    score = 0,
    weapon,
    vehicle,
    kit
}

export enum LeaderboardScoreType {
    overall = 0,
    risingstar,
    commander,
    team,
    combat
}
